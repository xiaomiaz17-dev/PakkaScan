import { randomBytes } from "node:crypto";
import type { WorkspaceProperty, PropertyWorkspaceStatus } from "./types";
export function createWorkspaceProperty(input:{userId:string;label:string;jurisdiction:string;now?:Date}):WorkspaceProperty { const now=(input.now??new Date()).toISOString(); if(!input.label.trim()) throw new Error("PROPERTY_LABEL_REQUIRED"); return {id:`prop_${randomBytes(8).toString("hex")}`,userId:input.userId,label:input.label.trim(),jurisdiction:input.jurisdiction,status:"DRAFT",createdAt:now,updatedAt:now}; }
const allowed:Record<PropertyWorkspaceStatus,PropertyWorkspaceStatus[]>={DRAFT:["UPLOADING"],UPLOADING:["PROCESSING","FAILED"],PROCESSING:["REVIEW_REQUIRED","REPORT_READY","FAILED"],REVIEW_REQUIRED:["PROCESSING","REPORT_READY","FAILED"],REPORT_READY:["PROCESSING"],FAILED:["UPLOADING","PROCESSING"]};
export function transitionWorkspace(property:WorkspaceProperty,next:PropertyWorkspaceStatus,now=new Date()):WorkspaceProperty { if(!allowed[property.status].includes(next)) throw new Error(`INVALID_WORKSPACE_TRANSITION:${property.status}->${next}`); return {...property,status:next,updatedAt:now.toISOString()}; }
export function attachReport(property:WorkspaceProperty,input:{verificationId:string;passportPublicId:string;now?:Date}):WorkspaceProperty { return {...property,status:"REPORT_READY",latestReportVerificationId:input.verificationId,passportPublicId:input.passportPublicId,updatedAt:(input.now??new Date()).toISOString()}; }
